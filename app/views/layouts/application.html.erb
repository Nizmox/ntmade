<!DOCTYPE html>
<html>
<head>
  <title>93 Made Games</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <!-- Added for Devise -->
  <% if admin_signed_in? %>
    <span>
        You are logged in as <%= current_admin.email %> - 
    </span>
      <%= link_to 'Logout', destroy_admin_session_path, :method => 'delete' %>
  <% end %>

  <p>
    <%= flash[:notice] %>
  </p>

  <p>
      <%= flash[:error] %>
  </p>

  <h1>93 Made Games</h1>

  <h3>Links</h3>
  <ul>
    <li><%= link_to 'Home', home_path %></li>
    <li><%= link_to 'Articles', articles_path %></li>
    <li><%= link_to 'Our Games', games_path %></li>
    <li><%= link_to 'Stockists', stockists_path %></li>
    <% if admin_signed_in? %>
      <li><%= link_to 'Setup', setup_path %>
    <% end %>
  </ul>

  <%= yield %>

  <p><%= Content.where("name = 'Footer'").first.body %></p>

</body>
</html>
