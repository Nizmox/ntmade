<!DOCTYPE html>
<html>
<head>
  <title>93 Made Games</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= controller_name %>">

  <!-- Added for Bootstrap -->
  <div class="container">

    <div class="row">
      <!-- Added for Devise -->
      <p>
      <% if admin_signed_in? %>
          You are logged in as <%= current_admin.email %> - 
          <%= link_to 'Logout', destroy_admin_session_path, :method => 'delete' %>
      <% end %>
        <%= flash[:notice] %> <%= flash[:error] %>
      </p>
    </div>

    <div class="row">
      <h1>93 Made Games</h1>
    </div>

    <div class="row">
      <!-- <h3>Links</h3> -->
      <ul class="nav nav-tabs">
        <li class="home"><%= link_to 'Home', home_path %></li>
        <li class="articles"><%= link_to 'Articles', articles_path %></li>
        <li class="games"><%= link_to 'Our Games', games_path %></li>
        <li class="stockists"><%= link_to 'Stockists', stockists_path %></li>
        <% if admin_signed_in? %>
          <li class="setup"><%= link_to 'Setup', setup_path %>
        <% end %>
      </ul>
    </div>

    <div class="row">
      <%= yield %>
    </div>

    <div class="row">
      <p><%= Content.where("name = 'Footer'").first.body %></p>
      <p style="font-size:11px">Site designed by Simon Taylor - 2013</p>
    </div>

  </div>

</body>
</html>
